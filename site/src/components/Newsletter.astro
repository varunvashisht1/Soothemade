---
interface Props {
  heading?: string;
  headingAccent?: string;
  body?: string;
}
const {
  heading = 'A letter, every other',
  headingAccent = 'Sunday.',
  body = 'One short note from the studio. A free printable in every issue. No tracking pixels, no upsells, no doorbell ringing.',
} = Astro.props;
---
<section class="newsletter">
  <div class="eyebrow-mark">— if you’d like to stay in touch —</div>
  <h2>{heading} <em>{headingAccent}</em></h2>
  <p>{body}</p>
  <form method="POST" action="/api/subscribe">
    <label class="visually-hidden" for="newsletter-email">Your email</label>
    <input
      id="newsletter-email"
      type="email"
      name="email"
      placeholder="your email"
      autocomplete="email"
      required
    />
    <button type="submit">Subscribe</button>
  </form>
  <div class="fine">Unsubscribe whenever · we never share addresses</div>
</section>

<style>
  .newsletter {
    background:
      radial-gradient(ellipse 700px 400px at 50% 30%, rgba(226, 167, 143, 0.18), transparent 65%),
      linear-gradient(170deg, var(--n-news-1) 0%, var(--n-news-2) 60%, var(--n-news-3) 100%);
    padding: 104px var(--sp-7);
    text-align: center;
    color: var(--ink);
  }
  .eyebrow-mark {
    font-family: var(--font-sans);
    font-size: 11px;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--accent-soft);
    margin-bottom: 14px;
  }
  h2 {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 34px;
    margin: 0 0 14px;
    color: var(--ink);
    font-weight: 300;
    line-height: 1.2;
  }
  h2 em { color: var(--accent-soft); }
  p {
    color: var(--n-paper-dim);
    margin: 0 auto 32px;
    max-width: 480px;
    font-size: 15px;
    line-height: 1.65;
  }
  form {
    display: flex;
    max-width: 460px;
    margin: 0 auto;
  }
  input[type="email"] {
    flex: 1;
    border: 1px solid var(--ink);
    border-right: none;
    background: transparent;
    padding: 14px 16px;
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 15px;
    color: var(--ink);
    outline: none;
  }
  input::placeholder { color: var(--n-paper-faint); }
  button {
    border: 1px solid var(--ink);
    background: var(--ink);
    color: var(--n-news-1);
    padding: 0 24px;
    font-family: var(--font-sans);
    font-size: 11px;
    letter-spacing: var(--ls-wider);
    text-transform: uppercase;
    cursor: pointer;
    transition: background var(--t-fast) var(--ease), color var(--t-fast) var(--ease);
    font-weight: 500;
  }
  button:hover { background: var(--accent); color: var(--ink); border-color: var(--accent); }
  .fine {
    color: var(--n-paper-faint);
    font-family: var(--font-sans);
    font-size: 11px;
    letter-spacing: var(--ls-wide);
    margin-top: 20px;
  }
  .visually-hidden {
    position: absolute;
    width: 1px; height: 1px;
    padding: 0; margin: -1px;
    overflow: hidden; clip: rect(0, 0, 0, 0);
    white-space: nowrap; border: 0;
  }
  @media (max-width: 880px) {
    .newsletter { padding: 72px var(--sp-5); }
    h2 { font-size: 26px; }
  }
</style>
